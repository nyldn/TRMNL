# /trmnl-new

Create a new TRMNL recipe scaffold.

Expected input: a slug such as `daily-quote`.

Run:

```sh
./scripts/new-recipe.sh <slug>
./scripts/validate-recipe.sh recipes/<slug>
```

Then summarize the created files and the fields that need customization.

