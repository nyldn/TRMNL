<div class="layout layout--col layout--center gap--small">
  <span class="label">{{ display_label | default: "Quote" }}</span>
  <div class="description">{{ quote.text | default: "Add a quote." | truncate: 80 }}</div>
</div>

